{
  "name": "tc-groups-client",
  "description": "The new Groups API Client",
  "version": "1.0.0",
  "scripts": {
    "start": "node clientTest.js",
    "lint": "standard",
    "lint:fix": "standard --fix",
    "test": "mocha -t 30000 test/*.test.js --exit",
    "cov": "nyc --reporter=html --reporter=text mocha -t 30000 test/*.test.js --exit"
  },
  "devDependencies": {
    "chai": "^4.1.1",
    "mocha": "^5.2.0",
    "nyc": "^14.1.0",
    "standard": "^12.0.1"
  },
  "dependencies": {
    "q": "^1.5.1",
    "request": "^2.88.0",
    "swagger-js-codegen": "^1.12.0"
  },
  "standard": {
    "env": [
      "mocha"
    ],
    "ignore": [
      "Client.js"
    ]
  }
}
